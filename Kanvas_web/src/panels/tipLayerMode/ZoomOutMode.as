package panels.tipLayerMode
{
	import panels.ToolTipLayer;
	
	public class ZoomOutMode extends TipLayerModeBase
	{
		public function ZoomOutMode(tipLayer:ToolTipLayer)
		{
			super(tipLayer);
		}
	}
}